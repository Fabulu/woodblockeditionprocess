# Extracted Paddle Text

These files are derived from the saved `paddleocr-ppocrv4/*.json` outputs by reading `res.rec_texts`.
They exist because the original saved `.txt` companions were empty on this machine even when the JSON contained OCR text.
