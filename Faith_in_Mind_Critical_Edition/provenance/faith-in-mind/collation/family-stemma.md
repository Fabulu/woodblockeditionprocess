# Faith in Mind Family Stemma Working Note

Date: 2026-04-14  
Status: provisional, witness set locked

This is the package-local family note used during collation planning.

The broader companion note remains:

- `FAITH_IN_MIND_STEMMA.md`

## Current practical family split

### Family A. Standalone branch

- `T1` = standalone `三祖大師信心銘`

### Family B. `四部録` branch

- `T2` = NDL `四部録`
- `T4` = Kyoto `四部録` (`1629`)
- `T5` = Kyoto `四部録` (`1631`)
- `A1` = Waseda `四部録`

### Family C. `四部録抄` branch

- `A2` = Waseda `四部録抄`
- `A3` = NDL `四部録抄`

### Family D. `入衆日用` branch

- `T3` = Kyoto `入衆日用`

### Family E. Translation / reception controls

- `C1`
- `C2`

### Family F. Commentary / study controls

- `C3` through `C17`

### Family G. Secondary source-tradition / anthology controls

- `S1` through `S5`

## Working reading

- Treat `T1` through `T5` as the main comparison spine.
- Treat `A1` through `A3` as close derivative / anthology witnesses.
- Treat `C*` and `S*` materials as control evidence, not copy-text candidates.

## Rule

Do not turn this into a directional stemma with stronger genealogical claims until:

1. the base witnesses have been directly inspected or OCRed far enough to compare the `信心銘` loci
2. anthology extraction boundaries are confirmed
3. the project has a first-pass variant table
