# Unresolved Loci

No durable text-level unresolved loci are recorded yet, and the render-preparation slice is now completed for the renderable held first-tier PDFs.

The current active open questions are:

1. Preserve the copy-text result that selects `YJG-W22` at witness level without pretending that its early shared-interior interference or `page-0063` line-tier uncertainty have already been solved away.
2. Tighten the newly reopened first-tier render subset from opening facts into honest closing or full-span facts:
   `YJG-W4C` now opens immediately on `page-0001`;
   `YJG-W2`, `YJG-W4F`, and `YJG-W4G` now open on `page-0005`;
   `YJG-W8` and `YJG-W9` now open on `page-0002`.
3. Preserve the newly exposed packaging split inside that subset:
   `YJG-W2` and `YJG-W4F` now plainly behave as commentary-bearing or prose-surrounded manifestations rather than clean poem-only codices, while `YJG-W4C` now looks like a short or differently packaged witness tranche.
4. Keep `YJG-W21` blocked honestly until the held PDF can be rendered or replaced by a genuinely workable local source file.
5. Keep `YJG-W12` blocked honestly rather than pretending it is a latent OCR quick win.
6. Keep `YJG-A10` and `YJG-A11` as second-tier controls outside the first-tier exact re-entry tranche.
