# Unresolved Loci

No completion-blocking text-level unresolved loci remain in the present poem-first Song edition.

The current non-blocking open questions are:

1. Preserve the copy-text result that selects `YJG-W22` at witness level without pretending that its early shared-interior interference or `page-0063` line-tier uncertainty have already been solved away.
2. Preserve the now-stabilized render-prepared first-tier span set:
   `YJG-W2` and `YJG-W4F` both open on mixed `page-0005` and close on mixed `page-0048`;
   `YJG-W4C` opens on mixed `page-0001` and closes on mixed `page-0021`;
   `YJG-W4G` opens on mixed `page-0005` and closes on mixed `page-0058`;
   `YJG-W8` and `YJG-W9` both open on mixed `page-0002` and close on mixed `page-0055`.
3. Preserve the grouped selective apparatus rather than over-promoting it into unearned full collation.
4. Keep `YJG-W21` blocked honestly until the held PDF can be rendered or replaced by a genuinely workable local source file.
5. Keep `YJG-W12` blocked honestly rather than pretending it is a latent OCR quick win.
6. Keep `YJG-A10` and `YJG-A11` as second-tier controls outside the first-tier exact re-entry tranche.
7. No item above blocks the present poem-first completion declaration unless the package is explicitly reopened for later revision.
