Continue the Faith in Mind critical-edition workflow from the current package state and do not stop after a single page or slice unless you hit a true package-level stop condition.

Operating requirements:
- Work inside C:\woodblocks\Faith_in_Mind_Critical_Edition.
- Read all state and gate files needed to resume safely before making changes.
- At minimum, load and follow:
  - provenance/faith-in-mind/process/current-state.md
  - provenance/faith-in-mind/process/human-log.md
  - provenance/faith-in-mind/process/process-log.md
  - provenance/faith-in-mind/process/correction-log.md
  - provenance/faith-in-mind/process/ocr-consensus-log.md
  - provenance/faith-in-mind/process/translation-diff-log.md
  - provenance/faith-in-mind/process/unresolved-loci.md
  - provenance/faith-in-mind/process/CONTINUATION_GATE.md
  - provenance/faith-in-mind/process/STATUS_REPORT_GATE.md
  - provenance/faith-in-mind/process/OCR_STOP_GATE.md
  - xml-open/ce/faith-in-mind/process.json
  - xml-open/ce/faith-in-mind/timeline.json
- Inspect any additional provenance, OCR, witness, transcription, or page-image files required to continue correctly.

Execution policy:
- Continue through the next required correction/comparison work and keep rolling forward slice by slice.
- Do not stop just because one page or one locus was finished.
- Do not stop just because one local page cluster or one current slice hits an evidence wall if other unresolved loci elsewhere in the package still remain potentially workable.
- Treat local evidence walls as state updates, not as terminal conditions, unless they collectively exhaust the entire remaining unresolved set.
- If the front-of-queue loci are blocked, roam to other unresolved loci already recorded in the package and keep pursuing any bounded productive slice that can still be completed honestly.
- Prefer exact local image reinspection, already-opened OCR support, and already-opened witnesses before declaring a locus terminal.
- Only stop when one of these is true:
  - the entire remaining unresolved package queue is actually exhausted by resolution or honest evidence walls,
  - a gate file requires stopping,
  - package validation fails and you cannot repair it safely,
  - you hit a genuine blocker that needs user input or missing external evidence.

Global stop test:
- Before stopping for an evidence wall, verify that no other unresolved loci in `unresolved-loci.md`, `current-state.md`, the corrected working text, or the live process state still offer a bounded productive slice on the present in-package basis.
- If any other unresolved locus remains potentially workable, continue there instead of stopping.
- Only report queue exhaustion when the remaining unresolved loci are all either:
  - already resolved,
  - explicitly paused by gate/policy,
  - or honestly below threshold after direct reinspection and already-opened witness/OCR support checks.
- If you stop at a real package-level wall, say so explicitly using package-level language rather than local page language.

Safety and persistence:
- Validate the package at sensible checkpoints.
- Keep provenance/state files synchronized with text changes.
- If a batch succeeds, make a commit before moving on to the next meaningful batch.
- If a batch fails or causes regression, revert only your own failed batch and continue from the last good state.
- Do not overwrite or revert unrelated user changes.

Finish condition:
- If you are not blocked, keep going.
- If you eventually stop, the final message must say exactly why you stopped and what remains, if anything.
- If you stop because the package is exhausted on the present evidence basis, say explicitly that the stop is package-level and list the remaining unresolved loci.
